# California Ballot Measures - Python Dependencies
# Core dependencies for the reorganized project

# Web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
playwright>=1.40.0  # JS-rendered registrar sites (Riverside Cloudflare challenge); needs `playwright install chromium`
boto3>=1.34.0  # Cloudflare R2 (S3-compatible) artifact store

# Data processing
pandas>=2.1.0
numpy>=1.24.0
openpyxl>=3.1.0  # For Excel file support
xlrd>=2.0.0      # For older .xls files

# API Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
python-multipart>=0.0.6  # For form data

# Environment and configuration
python-dotenv>=1.0.0

# AI/LLM
anthropic>=0.18.0  # Claude (paid)
groq>=0.4.0  # Groq (free)
ollama>=0.1.0  # Ollama (local, free)

# Development tools (optional, but recommended)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.9.0
flake8>=6.1.0